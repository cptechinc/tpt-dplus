<div class="list-group">
	<div class="list-group-item bg-dark text-white">
		<div class="row">
			<div class="col-4">
				Vendor Item
			</div>
			<div class="col-4">
				Our Item
			</div>
			<div class="col-4">
				Description
			</div>
		</div>
	</div>
	{% if items.count() %}
		{% for item in items %}
			<div class="list-group-item">
				<div class="row">
					<div class="col-4">
						<a href="{{ page.cxm_itemURL(item.custid, item.custitemid) }}" class="link">
							{{ item.vendoritemID }}
						</a>
					</div>
					<div class="col-4">
						{{ item.ouritemID }}
					</div>
					<div class="col-4">
						{{ item.description }}
					</div>
				</div>
			</div>
		{% endfor %}
	{% else %}
		<div class="list-group-item text-center">
			<h5 >No CXM Items Found</h5>
		</div>
	{% endif %}
</div>
